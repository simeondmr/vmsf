#include <stdio.h>
#include "../include/opcodes.h"
#include "../include/registers_flags.h"

int execute(unsigned char *code) {
  
/*	while (NOT END) {
		//opcode = fetch(code)
		switch(opcode) {
		case NOP:
			break;
   
	  
		}
	}
*/
  return 0;
}
